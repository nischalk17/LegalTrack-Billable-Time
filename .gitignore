node_modules/
.next/
.env
dist/
*.log
.DS_Store
dist
build
.next
coverage
docker-compose.override.yml

# ── Dependencies ─────────────────────────────
node_modules/
.pnp
.pnp.js

# ── Next.js ───────────────────────────────────
.next/
out/
frontend/.next/
frontend/out/

# ── Build outputs ─────────────────────────────
dist/
build/
*.exe
*.dmg
*.deb
*.AppImage
*.snap
*.blockmap
archive/electron-app/dist/
archive/electron-app/build/installer/

# ── Electron ──────────────────────────────────
archive/electron-app/out/

# ── Environment files ─────────────────────────
.env.*
backend/.env
archive/tracker/.env
archive/electron-app/.env

# ── Sensitive / secrets ───────────────────────
*.pem
*.key
*.p12
*.pfx
*.pgpass
*credentials*
*secret*
.npmrc
.netrc
.aws/
.ssh/

# ── Logs ──────────────────────────────────────
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
logs/

# ── OS files ──────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini

# ── Editor ────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo

# ── Electron-store (encrypted local data) ─────
archive/electron-app/*.json
!archive/electron-app/package.json
!archive/electron-app/package-lock.json

# ── Icons (binary files, add manually) ────────
# archive/electron-app/assets/icon.ico
# archive/electron-app/assets/icon.png
# archive/electron-app/assets/icon-paused.png
# (comment above lines OUT once you add real icons)

# ── Misc ──────────────────────────────────────
.cache/
.turbo/
.vercel/

# ── AI tooling / local dev config (not part of the product) ─
.claude/
.agents/
skills-lock.json
.cursorignore
AI_DEVELOPMENT_LOG.md

